# ==========================================
# 1. LOCAL DATA STORAGE
# ==========================================
# Absolute paths to where data will be stored on your machine.
# - RAW: Downloaded files (images, ecgs, csvs)
# - PROCESSED: Intermediate features, tokens, splits
# - MODELS: Saved model weights and checkpoints
RAW_DATA_DIR=/absolute/path/to/project/data/raw
PROCESSED_DATA_DIR=/absolute/path/to/project/data/processed
MODELS_DATA_DIR=/absolute/path/to/project/data/models

# ==========================================
# 2. DATABASE CONFIGURATION (Postgres)
# ==========================================
# Credentials for the local Postgres database hosting MIMIC-IV
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mimiciv
DB_USER=your_local_db_user
DB_PASSWORD=your_local_db_password

# ==========================================
# 3. PHYSIONET CREDENTIALS
# ==========================================
# Required to download restricted MIMIC-IV data via scripts.
# Ensure you have signed the DUA on physionet.org.
PHYSIONET_USER=your_physionet_username
PHYSIONET_PASS=your_physionet_password